---
import BaseLayout from '../layouts/BaseLayout.astro';
import { localizedPath } from '../lib/site';

const quickLinks = [
	{ href: localizedPath('zh', '/'), label: '首页' },
	{ href: `${localizedPath('zh', '/')}#leaderboard`, label: '榜单' },
	{ href: localizedPath('zh', '/benchmarks/'), label: 'Benchmarks' },
	{ href: localizedPath('zh', '/blog/'), label: '博客' },
];
---

<BaseLayout
	lang="zh"
	title="404"
	description="这个页面没有找到。回到 Lens Frontier 的榜单、Benchmark 和研究发布。"
	alternatePath="/"
>
	<section class="not-found" aria-labelledby="not-found-title">
		<div class="not-found__code" aria-hidden="true">
			<span>4</span>
			<span>0</span>
			<span>4</span>
		</div>
		<div class="not-found__body">
			<p class="eyebrow">missing measurement</p>
			<h1 id="not-found-title">这个坐标没有记录。</h1>
			<p>
				可能是链接移动了，也可能是这项内容尚未公开。先回到模型榜单、Benchmark 目录或研究发布。
			</p>
			<nav class="not-found__links" aria-label="404 shortcuts">
				{quickLinks.map((item) => <a href={item.href}>{item.label}</a>)}
			</nav>
		</div>
	</section>
</BaseLayout>
